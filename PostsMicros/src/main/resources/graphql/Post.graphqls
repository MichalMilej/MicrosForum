type Query {
    getUserPosts(userId: String!) : [Post]
}

type Post {
    id: ID!
    userId: String
    text: String
    comments: [Comment]
}

type Comment {
    id: ID!
    userId: String
    text: String
}